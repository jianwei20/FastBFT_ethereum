- hosts: geth-nodes
  tasks:
    - name: run experiment.
      command: bash /home/ubuntu/FastBFT_ethereum/experiment/aws_run.sh "{{total}}" "{{size}}" "{{each}}"
      args:
          chdir: /home/ubuntu/FastBFT_ethereum/experiment
      register: shell_result
    - debug: msg="{{ shell_result.stdout }}"
